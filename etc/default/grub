# /etc/default/grub: controls the operation of grub-mkconfig.
#
# If you change this file, run update-grub or "grub-mkconfig -o /boot/grub/grub.cfg" to
# update /boot/grub/grub.cfg (GRUB menu). For full documentation of the options, see:
# https://www.gnu.org/software/grub/manual/grub/html_node/Simple-configuration.html
#
# If your computer has multiple operating systems installed, you might want
# to run os-prober. However, if your computer is hosting operating systems
# installed via LVM or raw disk devices, running os-prober could damage
# these operating systems as it mounts filesystems for probing.
# GRUB_DISABLE_OS_PROBER=false
#
# Uncomment to enable BadRAM filtering; modify to suit your needs. This works
# with Linux (no patch required) and with any kernel that obtains the memory
# map information from GRUB, such as the GNU Mach and the FreeBSD kernel.
# GRUB_BADRAM=0x01234567,0xfefefefe,0x89abcdef,0xefefefef
#
# Uncomment to disable graphical terminal.
# GRUB_TERMINAL=console
#
# The resolution used for graphical terminal; only modes supported
# by your graphics card via VBE can be used. You can check these
# modes in GRUB command prompt using the command vbeinfo.
# GRUB_GFXMODE=640x480
#
# Uncomment if you want GRUB to avoid the UUID= format for Linux boot parameters.
# GRUB_DISABLE_LINUX_UUID=true
#
# Uncomment to disable the generation of recovery mode menu entries.
# GRUB_DISABLE_RECOVERY=true
#
# Uncomment to get a beep when GRUB starts.
# GRUB_INIT_TUNE="480 440 1"
#
# Customizing the Linux boot parameters and the GRUB menu appearance:
GRUB_BACKGROUND=/usr/share/wallpapers/Next/contents/images_dark/3200x1800.png
GRUB_CMDLINE_LINUX="quiet systemd.show_status=1"
GRUB_CMDLINE_LINUX_DEFAULT="resume=UUID=ca0883be-e260-44f0-a33f-56785ed8734e resume_offset=55646208 systemd.swap-extra=/swap.img"
GRUB_CMDLINE_LINUX_RECOVERY="noresume single systemd.swap=0"
GRUB_DISABLE_SUBMENU=true
GRUB_DISTRIBUTOR=Ubuntu
