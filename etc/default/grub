# /etc/default/grub: controls the operation of grub-mkconfig.
#
# If you change this file, run update-grub or "grub-mkconfig -o /boot/grub/grub.cfg" to
# update /boot/grub/grub.cfg (GRUB menu). For full documentation of the options, see:
# https://www.gnu.org/software/grub/manual/grub/html_node/Simple-configuration.html
#
# If your computer has multiple operating systems installed, you may want
# to run os-prober. However, if your computer hosts operating systems
# installed using LVM or raw disk devices, running os-prober could
# damage these installations, as it mounts filesystems to probe.
# Uncomment to enable os-prober (it is disabled by default).
# GRUB_DISABLE_OS_PROBER=false
#
# Uncomment to filter out bad RAM; modify to suit your needs. This works with
# Linux (no patch required) and with all kernels that obtain the memory map
# information from GRUB, such as GNU Mach and the FreeBSD kernel.
# GRUB_BADRAM=0x01234567,0xfefefefe,0x89abcdef,0xefefefef
#
# Uncomment to disable graphical terminal.
# GRUB_TERMINAL=console
#
# The resolution used for graphical terminal; only modes supported
# by your graphics card via VBE should be used here. You can list
# these modes in GRUB command prompt using the vbeinfo command.
# GRUB_GFXMODE=640x480
#
# Uncomment if you want GRUB to avoid the UUID= format in Linux parameters.
# GRUB_DISABLE_LINUX_UUID=true
#
# Uncomment to disable the generation of recovery mode menu entries.
# GRUB_DISABLE_RECOVERY=true
#
# Uncomment to get a beep when GRUB starts.
# GRUB_INIT_TUNE="480 440 1"

# All boots: selecting deep sleep to enable true suspend-to-RAM.
GRUB_CMDLINE_LINUX=mem_sleep_default=deep

# Default boots: specifying swap file to enable resume (using offset, root is found by UUID=);
# specifying swap file again to enable swap space (using file path, root is mounted at /).
GRUB_CMDLINE_LINUX_DEFAULT="\
resume=UUID=ca0883be-e260-44f0-a33f-56785ed8734e \
resume_offset=55646208 \
systemd.swap-extra=/swap.img"

# Recovery boots: not loading resume images; booting as
# single user (rescue mode); not mounting swap units.
GRUB_CMDLINE_LINUX_RECOVERY="\
noresume \
single \
systemd.swap=no"

# Displaying all the GRUB menu entries at once; labeling the
# operating system as Ubuntu to agree with theming scripts.
GRUB_DISABLE_SUBMENU=true
GRUB_DISTRIBUTOR=Ubuntu
